#include "pvco.h"

#include <QGraphicsScene>
#include <QSlider>

PVCO::PVCO()
{
    /* FIXME
    QSlider* waveSelector = new QSlider(Qt::Horizontal);
    waveSelector->setTickPosition(QSlider::TicksBelow);
    waveSelector->setRange(0, 3);
    m_waveSelector = scene()->addWidget(waveSelector);
    */
}
